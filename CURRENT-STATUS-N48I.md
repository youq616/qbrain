# Qbrain 当前状态

## N48I：配对任务费用对照

2026-09-25（Asia/Seoul）。受测提交
`3880dde71190b15af7f9b4831ecfc02a6e433fef`，树
`efcaae2f25bd55fa611a7491a284f5551ff55172`。
以已合并 N48H 的 main `b2ae3380` 为基线；实际合并身份见
[PR50](https://github.com/youq616/qbrain/pull/50)。收尾文档提交不是程序构建来源。

原生命令 `cost compare` 连接两份规范化账本与同一组任务。每条已提交调用必须归属
某个任务或共享开销；主模型/主价格表和声明的共同条件保持可比。失败重试与辅助
调用均计入，未知或不完整时全组差额留空。总费用差额是带符号的精确金额，比例
是精确约分分数，基准为零时比例未知。两份导入输出可不改 cost_input 直接衔接。

## 固定源码原生验收已完成

N48I 专项运行36140168439的 Windows/Linux 任务均成功；既有 N48D 回归36140207308
的两平台任务也成功。均为 attempt1，未靠重试、延长产品超时或削弱断言通过。
每平台新144项直接检查、普通/优化模式各288次独立调用、20种报告篡改拒绝通过；
每平台55步驱动完成原计费/导入/流式/生命周期/MCP及应用回归，Windows原60组通过。

两份原始工件逐一核对源码、程序和日志摘要，24组计费、12组生命周期及2组MCP报告
重新校验通过。每份1379源码文件一致，Windows仅1364文件精确换行转换差异。
N48I 源码模块验收完成；本结论不等于已发布新安装包或真实模型效果已经验证。

## 本人分离自审

实现后进行代码、计划对照、独立输入和结果复核。144 项直接检查及 ASan/UBSan
通过；288 个进程用例每模式通过，20 种篡改拒绝。补充独立审核 65 次调用/292 项
检查，另六个大小/归属边界探针、三次实际导入衔接、六个示例全部通过。
额外 65 次探针也在下载的 Linux CI 完整程序上复跑通过。Windows 结果只来自
真实 Windows CI，不冒称用户本机执行。没有削弱旧测试来消除失败。

本轮首个测试编译发现命名空间限定遗漏，在候选提交前修复；原失败日志保留。
已审范围内未发现遗留阻断问题，不保证绝对无缺陷。审核是当前用户授权的本人
分离工程自审，不是子代理、Claude Code 或第三方认证。
[完整审核](docs/nodes/N48I-HARD-AUDIT.md) ·
[精确结果与身份](docs/nodes/n48i-evidence/RESULT.json) ·
[使用说明](docs/integration/PAIRED-COST-COMPARISON.zh-CN.md)。

## 不能由本模块替代的验收

费用差额不是质量保持的节省结论。任务/条件摘要和 ledger_complete 是调用方声明，
不是独立遥测；未提交的调用、模型真实身份、费率适用性、账单、税费和汇率均未认证。
本轮没有实际付费模型、私人会话或脑库操作，也未自动适配 N47S 的 run.json。

真实客户端后续记忆消费、同条件模型正确率/费用对照、集成 Windows 候选包、
PG 对等、完整 ACL/DLP、规模性能与签名仍需后续证据。Issue40 根因未知。
公开下载仍为旧 N47X，无新 Release/tag，不用旧 EXE 验证新命令。
[路线](docs/COMPLETION-ROADMAP.md) · [前阶段状态](CURRENT-STATUS-N48H.md)。
