# Qbrain 当前状态

## N47U：可逐条审计使用回执，源码验收完成

2026-09-19（Asia/Seoul）。新增 `fact usage-list --id ID`，在现有 memory_read 中
使用 view=usage_receipts。此前只能查使用次数，现在可以定位具体 usage_id、事实
版本和撤回状态，再由有写权限的调用方明确选择已有的 revoke-use。

验收源码 `b7f93bdce18cfc28e48d4cc7d1ef06a32d901831`，源树
`b7b00cff4bc5dd004ab25889aa5fd366568ceb1e`；[PR #38](https://github.com/youq616/qbrain/pull/38)
记录实际合并身份。不要把后续文档/合并提交当作这份原生构建的来源。

## 实际功能和边界

支持全部、当前版本、历史版本、已撤回筛选，按回执 ID 升序返回完整元数据行。
每页1–50条、JSON预算512–32768字节；预算不足则明确拒绝，不返回无法推进的空页。
分页须同时提交上一页游标和集合指纹。插入、撤回或版本变化会拒绝旧分页，要求
从首页重新查询，避免混用新旧状态导致漏项。指纹不是权限、签名或跨请求锁。

每页先校验完整的有界记录集再筛选，页外损坏不能被隐藏。过期、已退休或已遗忘
事实不能用旧游标继续读取。对已有脑库的查询不新增使用记录、初始化可选模块、
生成备份或修改事实；原CLI通用打开行为不变。没有改schema、写权限、Hook或安装器。
所有记录仍是 caller_reported，不证明真实模型使用或事实为真，不改变排名和衰减。

[命令与完整分页示例](docs/integration/FACT-USAGE-AUDIT.zh-CN.md) ·
[原上报/撤回规则](docs/integration/FACT-USAGE.zh-CN.md)。

## 实际验证和本人分离自审

固定 [35445385260](https://github.com/youq616/qbrain/actions/runs/35445385260) 为push/attempt1，
Windows/Linux两任务及所有必需步骤成功，使用各自重新编译的新程序。

| 范围 | 结果 |
| --- | --- |
| 新分页审计 | 两平台各71检查、111命令、333份原始命令流核验 |
| 原使用记录 | 两平台各75检查、118命令通过 |
| 既有进程回归 | 事实34、上下文65、多词112、参数60、搜索226、Hook事实52、批量生命周期40、记忆44、MCP17通过 |
| 原生单元回归 | Windows60组逐项核对；Linux4/4核心组；真实PG仍SKIP |
| 另行边界复核 | 下载的Linux程序在普通/优化Python各14检查、215调用通过，含64种分页组合、自然过期及只读快照 |

两份原始工件、1140文件源码树、不同平台EXE/脚本哈希、页面内容与原始日志已回读。
额外探测仅在Linux执行，不冒称用户Win11或已登录客户端。补充测试最初误用按原话
去重的记忆视图定位独立证据，已修正夹具并保留失败；原产品及原测试断言未削弱。

**限定源码功能范围本人分离工程自审PASS，未发现未解决阻断项。** 不冒称其他代理
或第三方，不保证绝对无缺陷。[完整审核](docs/nodes/N47U-HARD-AUDIT.md) ·
[精确证据](docs/nodes/n47u-evidence/SUMMARY.json)。

## 下载和剩余项目阶段

N47R公开包不变，不含N47T/N47U命令；新功能需要新源码构建，本轮不发布或替换
Release/tag，也不要求用户现在重新安装或转交本地Agent。

项目仍在集成预览后的功能完善与实际效果验收阶段。v1条件估计仍3–4个实质回合，
完整增强路线仍粗估15–25回合且包含v1；这是必要环境可用后的规划，不是逐消息倒数。
真实客户端、模型对照、费用与PG门槛没有被回执查询替代，通用语义、确认、画像和
衰减也未因本功能完成。[完整路线](docs/COMPLETION-ROADMAP.md) ·
[上阶段状态](CURRENT-STATUS-N47T.md)。
