# Qbrain 当前交付状态

## 已合并并发布：N47H 分页发现待归档／恢复事实

[PR #24](https://github.com/youq616/qbrain/pull/24) 已合并，合并提交
`21f0c7f9def3c59aee9093ac5c675a3377caa293`。实际产品／原生测试源码为
`80fe1b9d31de6cc41d06be6db0e1035c1a747eb7`，源树
`5d17b1f68344d1c6370d4a113fddf5def1dc0e63`。
审核头仅增加／更新 docs 下7项文档和补充探针证据，不改变生产代码、原测试、
构建或交付字节；合并提交与审核头无文件差异。后续说明更新不代表再次编译。

[预览 Release：candidates-preview-80fe1b9d](https://github.com/youq616/qbrain/releases/tag/candidates-preview-80fe1b9d)
已经公开。产品 `qbrain-windows-x64-candidates.zip`，2,044,265 字节，SHA256：
`6d4747d0332fc39e45a5ca4b2e815ee01e135959b97722ae92287dbaa14508cd`。
完整解压并阅读 `LIFECYCLE-CANDIDATES.zh-CN.md`。这是原CI测试字节、未重打包、
未签名的开发预览版，不是整个融合项目完成。预编译产品无需本机编译器。

## 实际能力与调用边界

`fact candidates` 和既有 `memory_read(view=lifecycle_candidates)` 可以分页寻找
当前来源的维护候选。operation=archive 选择有效active、未归档且最新有效支持
创建年龄达到阈值的事实；operation=restore 选择有效active已归档事实。年龄不
等于最后使用、用户确认、可信度或真假。unknown／未来时间不当作陈旧。

结果仅包含metadata与N47G兼容batch_payload，不复制原话、不自动写入。读取
不创建表、备份或模型任务；没有新增MCP工具名、安装默认开关或权限。

二进制fact_id顺序和after_id支持继续查看，每页最多检查100个原始候选、返回32项。
预算中断不会越过未返回的当前条目。空页可能仍有后续；progressed=false时应提高
预算或明确停止，不能无条件重试，也不能把空页当成“没有陈旧记忆”。每页一个
读快照，不是跨页稳定全库快照；已越过的较小ID发生新变化时须重新开始扫描。
游标只是寻址边界，不是授权或预约。输入／结果／证据预算不是SQL或I/O时延保证。

候选清单需明确交给N47G batch-preview／batch-apply。实际执行重新校验全部证据、
状态和expected_revision，任何一项失效整批拒绝。不要自动改版本号绕过冲突。
归档非隐私删除，有效直接反证仍保留；恢复不能复活退休、过期、遗忘或损坏内容。

## 已执行验证与单独审核

开发35111907426和N4235111907432必需任务全部通过：Windows精确56注册组；候选
17场景／647断言在Server2025、Server2022、portable及Linux ASan+UBSan执行通过；
实际CLI/MCP43检查／63次预期退出（Windows57次exit0、6次负向exit1）。旧事实、
批量、生命周期、自动整理、Hook、召回、冲突、HTTP、队列、CJK及双PowerShell门槛
保留通过。真实PostgreSQL DSN仍SKIP-PG，不能计作PG集成验收。

协调方从同源码重新GCC14.2编译并执行候选、原生命周期、原批量和141报告／注册测试。
另外独立建模288个混合状态事实，完成46次遍历／695页和12107断言（含332准备断言），
验证预算续查、完整候选集合以及边分页边明确应用时不漏不重。刻意推进错误游标的
临时副本准确触发原断言失败，正式源码未改变。6份原始Artifact通过254项回读，
重建811文件精确源码树并验证完整报告与交付字节。探针二进制未另下载，身份绑定
固定原报告及原打包的实际探针检查；此边界在审核报告中保留。

本阶段未发现未解决P0/P1，不保证排除所有潜在缺陷。审核是所有者授权的单独工程
自审，不冒称独立子代理或第三方，也没有新增已登录客户端实机测试。

发布35116062175成功：绑定已合并PR、固定源码／审核、原始CI与Artifact摘要，重验
报告后创建新草稿，下载逐字节核对三项资产再公开。不重编译、重打包或覆盖旧版，
不启用通用未审核自动发布。

[阶段审核](docs/nodes/N47H-HARD-AUDIT.md) · [测试摘要](docs/nodes/n47h-evidence/SUMMARY.json) ·
[发布记录](docs/nodes/n47h-evidence/RELEASE.json)

## 既有能力和未完成范围

N47G显式原子批量、N47F可撤销单条归档及只读年龄、N47E本地规则自动事实整理、
N47D有明确开关的Hook事实召回、N47A-C证据绑定／显式冲突／含反证查询保持。
首次归档模块准备可留下备份或空表，但批量策略更新仍是单事务；旧二进制忽略
归档策略。基础会话采集、来源隔离、遗忘、可撤销安装与旧检索行为保持。

N47D原实机证据已归档；所有者转交N47E真实Claude自动链路摘要，与本轮程序测试
分别记录，不冒称本轮收取其原始ZIP或重新验证Codex。Codex旧认证阻塞保持。

通用语义合并／冲突推断、使用确认计数、自动衰减归档、画像、百万事件性能保证、
PG对等、完整ACL/DLP、模型质量费用和正式签名仍未完成。候选发现不是自动维护。
当前没有必须交给本地Agent的新任务；不重做N47E、不需编译器或GitHub写权限。
真正本机任务仍只交一个完整提示词，所需文件先放仓库，给固定来源、哈希和用法。
