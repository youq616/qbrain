# Qbrain 当前状态

## N47Z：原子批量回执模块已完成跨平台源码验收

记录日期2026-09-20 UTC。批量预览、上报、撤回、快照校验、幂等结果识别和整批回滚
已经完成。实际验收源码为 `7898dec68b5162b33a193cb30295bdd1744e5269`，源树
`83312db93cd4302c2e98cfbc92b0266b38939be3`；[PR44](https://github.com/youq616/qbrain/pull/44)
记录最后合并身份，不以文档或合并提交冒充二进制来源。

此前仅在本地的N47Z已经提交到仓库；原计划写入阻塞由正常工具调用恢复，未绕过。
本次接续已有PR44，完成原生证据回读、附加独立账本对照与最终审核，不重复造模块。

## 完整模块能力

一次在同一来源内处理最多32条回执、8个事实。先只读预览，后用当前snapshot明确
执行；批次内任一写入或提交失败，回执变更全部回滚。实际成功变更会让旧批准失效，
重新预览后已完成的项显示为无需修改，不重复计数或改写时间。预览不授予写权限。

审核中的事务所有权缺口已经修正：不能仅凭autocommit判断连接空闲；批量执行会
拒绝调用方持有的显式或隐式事务，不替调用方提交或回滚未完成语句。只读预览仍可用。
原表结构、单条命令、权限和默认行为保持不变。回执仍只是调用方陈述，不证明消费
或真相，不自动调整排序、画像和衰减。[完整说明](docs/integration/USAGE-BATCHES.zh-CN.md)。

## 实际测试与分离自审

固定push/attempt1 [35516145384](https://github.com/youq616/qbrain/actions/runs/35516145384)
的Windows/Linux两任务及所有必需步骤均成功，执行各自重新编译的程序。

| 范围 | 结果 |
| --- | --- |
| 批量端到端 | 两平台普通/优化Python每组89检查、165调用、495份原始流核验 |
| 独立C++事务测试 | 两平台实际执行18项基础和41项所有权/提交失败检查，不以原60组代替 |
| 继承回归 | 完整性123、MCP边界72、使用75、分页71及九套既有进程测试保持通过 |
| 原生与核心构建 | Windows生产构建及60个原注册组通过；Linux6个CTest目标通过；真实PG仍SKIP |
| 本人另行参考账本 | 下载的Linux程序在普通/优化Python各执行24轮、8事实、125回执、656条MCP请求；汇总/分页与独立账本一致 |

两份原始工件和实际EXE、1231文件源树、测试脚本/报告/原始流已回读；Windows1217
文件仅精确LF转CRLF。12种批量报告篡改拒绝。附加复核在Linux执行，没有冒称用户
Windows本机或真实模型运行。历史候选的事务缺陷与修复记录保持，未放宽旧测试。

**限定模块范围本人分离工程自审通过，未发现未解决阻断问题。** 不是另一个代理
或第三方认证，不保证绝对无缺陷。[完整审核](docs/nodes/N47Z-HARD-AUDIT.md) ·
[精确证据](docs/nodes/n47z-evidence/SUMMARY.json)。

## 交付与剩余阶段

本轮没有创建或替换Release/tag；公开N47X包尚不含N47Y/N47Z修复和新增命令。
当前不需要用户重装、手动改库或发送密钥。本模块已完成源码与跨平台验收，但整个
项目仍有真实客户端消费、实际模型质量/费用、最终候选集成、PG对等和签名等门槛。
Issue40历史超时根因未因批量模块完成而关闭。

以三个v1验收工作流是否闭合报告进度，不把历史回合区间当逐消息倒计时。
[路线](docs/COMPLETION-ROADMAP.md) · [前阶段状态](CURRENT-STATUS-N47Y.md)。
