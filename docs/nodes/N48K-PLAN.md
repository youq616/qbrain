# N48K — usable Windows integration candidate

2026-09-26. Baseline main24345f85 / treeb9b5f8a5. Owner requests a complete module
and coordinator self-review after implementation. Scope: close the source/package
lag, not add more pricing or claim real client/model or stable-version acceptance.

Build the fixed current native source, keep its binary unchanged, and bundle the
installer/UTF-8 bridge, existing evaluation tools including N48J, current accounting
examples and guides. Use an explicit payload allowlist, canonical Git source bytes
(only PowerShell scripts receive CRLF), and a bounded deterministic ZIP. Never
replace old public release assets. Construction manifest is immutable and remains
unaccepted until an external receipt binds the exact tested ZIP.

Acceptance: packaging positive/adversarial tests in ordinary/optimized Python;
assemble twice byte-identically, independently verify all members, reject tampering
including self-rehashed manifests and unsafe Windows paths. Native Windows fresh
build + original60, retained55-step application gates against extracted EXE,
extracted N48J tests and original48 optional-evaluation tests. Both PowerShell5/7
must exercise extracted installation/recovery and N47X-to-current upgrade: preserve
facts, receipts, unrelated host settings, default-brain choice, explicit permission
semantics and uninstall data preservation. New package cannot be edited to pass.

No mandatory Python runtime, Docker/WSL, automatic capture or silent reinstall.
No paid requests, signing claim, PG parity, real signed-in host/model proof or
Issue40 closure. Review outcome only after actual logs/artifacts are available;
any failures remain evidence, and changed candidate code needs fresh validation.

## Acceptance closure — 2026-09-26

Done for the bounded N48K Windows integration candidate. Original full
qualification f261896e passed, and separate review identified and closed the
preexisting-receipt test gap through native runs36235526784 and36236062404.
Both Windows images and PowerShell5/7 retain complete old receipt snapshots
through upgrade, rollback, re-upgrade and uninstall. Package58d56b7b stays
unchanged; external acceptance and N48K-HARD-AUDIT.md record exact identities.
Actual merge is recorded by PR52. No real-client/model, PG/signing, stable-v1
or Issue40 completion is inferred. Earlier failures remain in the history.
