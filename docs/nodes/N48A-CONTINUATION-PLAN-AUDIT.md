# N48A continuation plan review

2026-09-21. APPROVED for repository implementation and native validation.
Reviewer: coordinating ChatGPT in a separately performed engineering plan pass,
explicitly authorized by the current user; not another subagent or third party.

The prior module is a complete local implementation, not native acceptance. It
must be submitted without mislabelling the prior local evidence, then tested on
Windows with all old suites retained. Direct generated MCP commands cannot certify
OpenCode's config loader. Both docs and actual pinned host results, where executed,
need separately recorded version/OS scopes. No model key or user login is needed
for isolated schema/MCP discovery; no blanket claim about every OpenCode release.

Potential audit targets include YAML env scalar types, platform-specific parser
count differences, Windows file/path/ACL behavior, filesystem journal ownership,
JSONC byte preservation and inherited environment write opt-in. Private original
config backups are not safe diagnostic uploads. Preserve unknown partial files.

The native feature and full C++ regression must pass before source merge. Real
model consumption, external client lifecycle, PG, signing and Issue40 root cause
remain outside what mock/loopback or schema checks can establish. No plan blocker
was found in this bounded continuation; final outcome remains pending.
