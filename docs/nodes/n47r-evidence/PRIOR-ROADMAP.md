# Qbrain 完成路线与回合估计

更新：2026-09-19。基于主路线Issue #2、N47O公开预览和N47P安装可靠性源码验收。
这是工程规划估计，不是承诺或后台排程。“回合”指一次实质开发、测试、自审与
入库，不是一次工具调用。上一4–6回合评估按原字节保留于
[n47p-evidence/PRIOR-ROADMAP.md](nodes/n47p-evidence/PRIOR-ROADMAP.md)。

## 当前定位与本轮进展

核心工程预览已公开，正在向日常可用v1验收收口。保留Windows原生C++20、PowerShell、
默认SQLite，不引入必需Docker/WSL服务。本轮N47P处理了中断恢复日志上限、完整
回滚前置校验、真实I/O中断重试和规划中途外部配置变化保护。

候选9feed7c7的Windows两版PowerShell各24项快照、60项恢复及原安装69/许可16/
传输8/事实33/整理33回归通过；全新原生60组通过。原始证据绑定固定源码，不能
把测试数量当作业务完成百分比。N47P没有发布新包，N47O下载里的安装脚本仍旧，
后续需要纳入集成交付。没有新增真实客户端消费、模型质量或PG验收。

## 日常可用v1：N47P之后预计还需3–5个实质回合

优先里程碑限定Windows11、SQLite、既有Claude/Codex接入、明确授权的采集/整理/
召回、原话来源与撤回机制、升级卸载及可诊断失败。必须以实际宿主和可复现任务
证明长期记忆能够被后续会话正确使用，不能仅用合成事件重放替代。

| 剩余工作流 | 预计回合 | 必须产出 |
| --- | ---: | --- |
| 安装修复的后续集成交付与必要边界收尾 | 0–1 | 纳入已验收N47P脚本，更新包与入口，不重复建立相同恢复验收 |
| 尚未覆盖的真实客户端生命周期与实际召回消费 | 1–2 | 复用既有通过证据，只补尚缺宿主/场景；登录与信任确需本机时只给一个完整提示词 |
| 代表性任务、性能/费用与v1最终验收 | 2 | 同资料同模型可复现比较、明确费用/正确率边界、版本集成与用户可用性证据 |

合计3–5回合，包含后续集成交付。这次调整来自安装可靠性工作的实际验收，不是
按消息条数扣减。真实客户端与效果目标没有被删去；新阻断问题需要修复并重验。
账号、客户端信任或原生宿主不可用时，验收仍待完成，不以假数据充数或承诺固定日期。

## 当前完整增强路线：仍粗估15–25回合，包含上述v1

这是不新增范围、必要宿主/模型/数据库条件可用、无重大架构返工的粗区间，
不等于所有未来版本gbrain/OpenViking或所有Agent永久等价。本轮安装修复不减少
下列主要不确定性，所以不机械将广义区间倒减一轮。

| 工作流 | 未完成范围 |
| --- | --- |
| PostgreSQL对等与数据生命周期 | 新memory/context/事实模块的真实PG实现、迁移/事务/来源隔离与集成验收；旧PG组PASS不代替新模块 |
| 通用记忆整理 | 更广泛中英文偏好、语义合并/冲突、确认和使用记录、衰减/画像；先保障证据、撤回和用户控制 |
| 权限与数据防泄露 | 完整可验证的ACL/DLP威胁模型与跨来源/项目边界；当前规则不是完整防泄露体系 |
| 更多宿主与兼容 | Cursor等明确需要的适配、真实生命周期和版本兼容；无限的未来Agent不纳入有限承诺 |
| 效果、性能与费用 | 代表性规模资料、缓存/增量策略、任务正确率及嵌入/摘要/提取/重排/重试全链路费用，根据数据决定ANN需求 |
| 正式发行 | 签名条件、可复现升级交付、回滚、文档及最终安全/回归验收；不假装已有证书和外部条件 |

出现数据完整性问题、外部验收阻塞或新增需求时重新估计并说明原因。主路线Issue #2
保持打开，阶段通过不等于全项目完成；也不能把广义剩余功能悄悄排除以宣布完成。

## 下一重点

优先补齐真实使用与代表性任务效果，并整合已验收的安装修复。先核对仓库中已接收
的宿主验收范围，不能跨产品版本/操作系统或跨项目套用证据；只让用户处理真正缺少
且必须本机执行的步骤。无需为本轮仓库开发额外重复登录或安装。
