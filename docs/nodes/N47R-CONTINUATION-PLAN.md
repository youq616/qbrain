# N47R continuation plan and separate plan review

2026-09-19. Reviewer: coordinating ChatGPT; owner-authorized separate self-review,
not another agent. Original identical plan write now succeeds; plan commit is
62a36e158eebcae73299e04f1d7f2e9a24628a8f. Prior safety failures remain historical.

Before submitting the implementation, make build-time versus later release
acceptance unambiguous in the guide/README/manifest. Do not claim native PASS in
a ZIP before testing it or rewrite tested ZIP bytes after approval. Keep initial
NOT_RUN/false manifest fields as construction-time observations; bind subsequent
acceptance/publication in an external receipt to the exact archive digest. The
installation guide may describe guarded use after a published receipt exists,
not claim the current candidate has been released. Regenerate and pin the new
ZIP hash, preserve the old local candidate/evidence, and rerun original tests.

Submit a read-only workflow with snapshot/recovery and all five legacy installer
suites against extracted bundle paths, both named Windows shells; package-tool
checks and both task event formats; fresh full native group verification. Add
report/hash validation against those same extracted files and retain source and
bundle identity. A score checker is not sufficient without runtime logs.

Review criteria and consent/runtime/ledger constraints remain the approved N47R
plan. Publication must use exact tested bytes and separate evidence and must not
be enabled until native evidence is inspected. No new paid model or real client.
