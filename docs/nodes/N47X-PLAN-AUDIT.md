# N47X separate plan review

2026-09-20. Reviewer: coordinating ChatGPT, owner-authorized separate plan pass.
Verdict: APPROVED for integrated preview delivery, not stable-v1 acceptance.

Five accepted source stages are missing from the public N47R binary/package. A
fresh native build and same-extracted-package tests are necessary; repackaging the
old EXE is not acceptable. Keep product and delivery commits separate. Require
fixed source inputs, a complete inventory and immutable post-test ZIP bytes.

Upgrade must preserve real synthetic facts created using the old public package,
and test new receipt operations after switching executable paths. Retain default
consent and uninstall behavior. Do not use unit counts alone as release authority.
Read-only validation precedes any write-enabled release step; archive failures and
bind all published assets to the observed accepted run. Strict source/asset identity
checks guard accidental substitution, not a malicious repository administrator.

No competing runtime feature, external provider call or user-machine handoff is
necessary. No known blocking plan finding remains. References checked: official
GitHub release/asset REST documentation and GITHUB_TOKEN least-privilege guidance.
The outcome audit must distinguish actual native execution, offline file checks,
public download integrity and missing real-model/client results.
