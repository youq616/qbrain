N37 D7 — Secrets final scan (whole working tree)
================================================
Date: 2026-08-16
Scanner: gitleaks NOT installed on this host. Equivalent pattern scan executed
         via Git Bash grep -rInE (text files only, --binary-files=without-match)
         over the working tree at D:\Projects\Qbrain, excluding .git/ and
         build/ (binary/object outputs). Supplement: `git ls-files` check for
         tracked .env files, case-insensitive private-key sweep, and sk-style
         key sweep outside tests/.
Scope: tracked files + untracked docs/evidence + scripts + third_party +
       gbrain-upstream/ (git-ignored reference checkout of the upstream
       inspiration repo; scanned anyway and recorded). dist/: DOES NOT EXIST
       at scan time (N37-A packaging outputs land later); if dist/ is
       produced after this scan, the parent should re-run this scan over it —
       packaging inputs are repo files already covered here, and the packaged
       zip contains no data-root content (per N37 plan security notes).

Pattern set (documented, gitleaks-generic-equivalent)
----------------------------------------------------
P1  -----BEGIN (RSA |EC |DSA |OPENSSH |PGP )?PRIVATE KEY( BLOCK)?-----   (also swept case-insensitive)
P2  AKIA[0-9A-Z]{16}                    (AWS access key id)
P3  gh[pousr]_[A-Za-z0-9]{36,}          (GitHub token)
P4  xox[baprs]-[A-Za-z0-9-]{10,}        (Slack token)
P5  AIza[0-9A-Za-z_-]{35}               (Google API key)
P6  (api[_-]?key|apikey|secret|token|passwd|password)["']?\s*[:=]\s*["'][^"']{8,}["']
                                        (generic credential assignment, case-insensitive)
P7  Bearer [A-Za-z0-9._+/=-]{12,}       (bearer literals)
P8  .env* / *.env files present         (environment secret files)
S1  sk-[A-Za-z0-9_-]{16,} outside tests/ and gbrain-upstream/ (supplement)

Results by pattern
------------------
P1 private-key blocks:          0 hits (exact + case-insensitive sweep)
P2 AWS access key ids:          0 hits
P3 GitHub tokens:               0 hits
P4 Slack tokens:                0 hits
P5 Google API keys:             0 hits
P6 generic credential literal:  9 hits (all assessed false positives, below)
P7 Bearer literals:             2 hits (both false positives, below)
P8 env files:                   1 file (placeholder-only example, below)
S1 sk- keys outside tests:      1 hit (self-referential evidence record, below)
Tracked .env files:             none

Per-hit assessment (every hit assessed)
---------------------------------------
P6-1  include/qbrain/jobs/minions.hpp:134  worker_token = "worker-local"
      Default parameter for the local in-process job worker identity label;
      a well-known constant, not a credential (no auth function).
P6-2  include/qbrain/jobs/minions.hpp:137  worker_token = "worker-local"
      Same as P6-1 (drain_jobs default).
P6-3  tests/test_n14.cpp:141  "secret":"payload-only"
      Synthetic JSON payload field name/value in a unit-test fixture; asserts
      payload contents are handled per policy. Not a credential.
P6-4  tests/test_n14.cpp:203  "result_secret":"must-not-appear"
      Redaction test fixture asserting the value never reaches output. Not a
      credential.
P6-5  tests/test_n17.cpp:1099  "secret":"N17_REMOTE_PAYLOAD_SECRET"
      Synthetic remote-payload fixture for the N17 negative matrix. Not a
      credential.
P6-6  tests/test_rerank.cpp:278  cfg.chat_api_key = "DUMMY_API_KEY_SENTINEL"
      Explicitly-named dummy sentinel proving fail-open rerank without an API
      key. Not a credential.
P6-7  tests/test_n33.cpp:518  embedding_api_key = "sk-n33b-closed-port-key"
      Synthetic key for the closed-port fail-open test (connection never
      leaves localhost). Not a credential.
P6-8  tests/test_n33.cpp:540  embedding_api_key = "sk-n33b-SECRETKEY"
      Synthetic fail-open credential-missing fixture. Not a credential.
P6-9  gbrain-upstream/.env.testing.example  DATABASE_URL=postgresql://postgres:postgres@localhost:5433/gbrain_test
      Standard local Docker default credentials for the upstream project's
      own example file; file is a template (.example) inside a git-ignored
      reference checkout, untracked. Not a secret.
P7-1  docs/nodes/n14-evidence/CLAUDE-N14-HARD-AUDIT-RESPONSE-FINAL-2.txt:27
      "Authorization: Bearer credential-value" — prose describing the
      redaction behavior (credential-value is a placeholder). Not a secret.
P7-2  tests/test_n14.cpp:660  "Authorization: Bearer credential-value"
      Redaction unit-test fixture with a placeholder value. Not a secret.
P8-1  gbrain-upstream/.env.testing.example
      Example env template of the upstream reference repo (untracked,
      git-ignored); contains only placeholders: sk-..., sk-ant-...,
      [project-ref]/[password] markers, and the localhost docker default
      above. Not a secret. No real .env exists anywhere in the tree.
S1-1  docs/nodes/n30-evidence/CHANGESET-MANIFEST.json:67  "sk-proj-0123456789abcdef"
      N30's own secrets-scan manifest quoting (as an exemption record) the
      synthetic dummy fixture tests/test_n14.cpp:675 which exercises the
      credential scrubber (key must NOT appear in job progress output).
      Self-referential evidence of a prior 99-hit/0-genuine scan. Not a
      secret.

Cross-reference
---------------
N30's pre-implementation scan (docs/nodes/n30-evidence/CHANGESET-MANIFEST.json)
covered 270 files with 99 raw hits / 0 genuine secrets; this final scan is
consistent with that result (no new credential values appeared; the only
overlapping artifact is the manifest's own exemption record).

Verdict
-------
ZERO genuine secrets in the working tree (all 13 raw hits + 1 env example
file assessed as false positives with reasons above). No tracked .env, no
private keys, no cloud-provider tokens. CI requires no secrets (public-repo
build only), per N37 plan security notes.

Caveat: dist/ absent at scan time — re-scan dist/ after N37-A packaging if it
is to be committed or published.
