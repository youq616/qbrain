# Actual host review findings and accepted fixture scope

2026-09-21. Coordinator separate engineering review; not another agent. Product source remains e419acf. First host run35613378046 stopped before any host execution because its sysfs directory observation belonged to the old mounted view. Replacing that observation with socket.if_nameindex plus a distinct current network-namespace identity retained the loopback-only gate. No network isolation was removed.

Run35613890950 then actually executed official OpenCode1.18.31 in a loopback-only network namespace. Both V1 read-only and explicit-write generated configurations completed debug config, actual MCP connected status, reconciliation of host-added $schema and explicit uninstall. The third scenario stopped because the test incorrectly assumed V1 compatibility lowering preserves V2 startup/catalog timeout. The downloaded failure artifact10644579552 (64130778bytes, SHA256695a39e6edfbac77af459ce83620f1618e795aa1e63a5d1261ede6f6d8888937) records two completed scenarios and the exact resolved configuration without timeout.

Official v1.18.31 packages/opencode/src/config/v2-compat.ts (blob9e4e0bf54089af28bf96db7e8825c12c92bec723) deliberately returns undefined from lowerTimeout when startup is present, and lowerServer deletes the original timeout. Therefore the product's valid V2 field cannot be described as fully honored by the V1 engine. Do not change correct V2 product settings, enable weaker defaults, or relabel the failed report as a pass.

Approved next test interpretation: continue the four exact host connection scenarios, require environment/command/access preservation in all, require scalar10000 only for V1, and separately record V2 timeout loss as an expected compatibility limitation. The final result may assert V1_HOST_VERIFIED only, with v2_engine_tested=false and v2_timeout_preserved=false. Actual V2 engine remains NOT_RUN, even if its configuration is accepted by the V1 compatibility reader. Keep this distinction prominently in user documentation; V1 users should choose --format v1. Core native acceptance does not depend on claiming full V2 host equivalence. No product source, original A/B/C assertion or native workflow is changed by this review-fixture correction.
