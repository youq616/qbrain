# Separate N48F completion review plan

2026-09-22. Owner-authorized coordinating ChatGPT self-review, not a subagent.
Continue existing PR47 at5990fbc; do not start a duplicate module. Retain all
product and original test bytes unless an actual blocking finding needs repair.

Independently compute the complete expected report with Python Fraction (not the
source-owned Decimal reference), including rates, provider/model metadata, strict
field/types, canonical digest, exact grouped totals and unknown propagation.
Exercise unknown/zero/rate-card combinations, valid mixed stage/outcome/retries,
512 calls/64 cards, canonical reorder/rate spellings, one-unit exact uint64 ceiling
and overflow, and nested escaped duplicate keys. Check report mutations that the
original reference did not separately name. Rerun actual downloaded Linux binary,
original cost suite and direct test; compile unchanged direct tests with sanitizers.
Read back original Windows/Linux artifacts and source-owned regression evidence.
This adds no actual provider price, model request, invoice or Windows execution.
