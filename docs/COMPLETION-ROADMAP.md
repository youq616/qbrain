# Qbrain 完成路线与回合估计

更新：2026-09-19。基于N47R实际发布及原生/升级/公开下载验收。
“回合”是一次实质开发、验证、自审与交付，不是一次消息或工具调用。
[上一版本路线](nodes/n47r-evidence/PRIOR-ROADMAP.md)保留原字节。

## 当前完成标准与已关闭工作

日常可用v1限定Windows11、SQLite、既有Claude/Codex接入、明确授权的采集/
整理/召回、原话来源与撤回、升级卸载和可诊断失败。Windows CI和合成Hook事件
可以证明工程行为，不能替代已登录客户端的实际消费或真实模型回答效果。

N47R已将N47P安装修复和N47Q评测工具纳入同一公开集成包；旧包到新路径升级
经过真实Windows文件/进程验证，五项发布文件通过公开前后及匿名下载检查。
这个交付缺口不再列为待办。此前“集成交付0–1 + 真实客户端1–2 + 效果/终验2”
的3–5回合粗规划，据此收敛为剩余3–4回合，不机械地按每条消息减一。

## 日常可用v1剩余：约3–4个实质回合

| 剩余验收工作流 | 估计 | 完成证据 |
| --- | ---: | --- |
| 尚缺真实客户端生命周期与记忆消费 | 1–2 | 复用有效实机记录，只补缺少的宿主/版本/场景；确认后续会话实际使用记忆，而非仅写出Hook输出 |
| 真实模型对照、代表性质量/性能/费用 | 1 | 相同模型与设置、新资料、隔离答案key和会话；真实请求响应及供应商用量，不用标准答案代替模型 |
| v1候选终验及必要修复 | 1 | 闭合前两项问题、最终版本/文档/升级验收；发现问题须修复重验，未通过不宣布稳定v1 |

估计以必要客户端、账号、权限及模型条件可用、需求不扩大、没有重大返工为前提。
外部条件不可用时验收保持待完成，不能以更多测试工具或模拟结果充数，也不承诺
固定日期。确需本机完成的步骤继续采用一个完整提示词交接，先发布所需文件。

## 当前完整增强目标：仍粗估15–25回合，包含上述v1

完整目标仍包含PostgreSQL新记忆模块对等、通用语义合并/冲突、确认和使用记录、
衰减/画像、完整ACL/DLP、更多明确需要的客户端、规模/性能/全链路费用优化及
正式签名发行。它们包含真实功能开发，不是仅剩打包；不承诺所有未来Agent永久等价。

本轮集成交付不能消除这些工作的主要架构和外部验收不确定性，因此保留粗区间，
不按小修复次数倒减。真实PG的SKIP、缺失客户端结果和未知模型费用保持明确；
主路线Issue #2继续打开。新需求或重大阻断出现时须说明原因并重新评估。

下一工作重心是真实使用和真实答案效果，不再重做已通过的包/安装场景来增加测试数。
当前无需用户为此交付阶段重复本机安装或登录。
