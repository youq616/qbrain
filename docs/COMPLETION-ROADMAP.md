# Qbrain 完成路线与阶段边界

更新：2026-09-20。N47Y使用回执完整性模块及MCP筛选/分页完成源码验收。
上一版[完整路线](COMPLETION-ROADMAP-N47X.md)按字节归档，不删除旧目标或改写
未完成的外部验收。“回合”指实质开发/测试/审核/交付，不是消息或工具调用次数。

## 已交付的工程基础

N47X已公开发布从b810固定源码重新编译的Windows工程预览，包含受控模型对照、
使用记录及审计、路径保护和桥接诊断。其整包/旧版升级/发布资产验证是已完成事实。
本次N47Y进一步统一回执完整性检查，修复MCP公布但入口拒绝的两个分页参数。
上报、重复重试、撤回、汇总和分页的完整操作范围已按最终源码通过Windows/Linux
回归和本人另行审查。正常语义不变，损坏则拒绝，不自动修复数据库。

N47Y新增修复暂为源码交付，N47X公开资产不变。后续候选集成须明确包含本次修复，
不能拿旧包当新修复验证，也不必每个源码改动重复独立发布。[当前状态](../CURRENT-STATUS.md)。

## 日常可用 v1 尚缺的验收工作流

| 工作流 | 必要证据 |
| --- | --- |
| 真实客户端生命周期与记忆消费 | 已登录目标客户端的后续会话实际使用；复用既有有效证据，只补缺失宿主/版本/场景 |
| 真实模型对照与质量/费用 | 新资料、相同模型设置、隔离答案文件和历史，实际请求响应及供应商用量 |
| 最终候选修复与集成 | 根据上述实际问题修复、完整回归、集成最终源码；未完成则不宣布稳定版 |

过去的v1约3–4个实质回合是必要账号、权限、客户端/模型环境已就绪、范围不扩大且
无重大返工时的条件区间，不是确定剩余次数。缺失外部条件时不会因多做一个内部
模块而自动消失。本轮没有新的真实模型/客户端结果，故不按测试数或消息数减一。
Issue40历史超时根因仍未知，不以回执或MCP校验测试代替它的实际诊断。

## 完整增强目标

PG新记忆模块对等、通用语义合并/冲突、确认与消费观测、使用驱动的画像/衰减、
完整ACL/DLP、更多明确需要的宿主、规模性能与全链路费用优化、正式签名发行仍
属于完整路线。使用回执可操作、可审计和完整性增强，不等于整个确认/画像组合
目标已经实现。真实PG仍有SKIP，真实模型/费用未执行值保持未知。

历史15–25回合包含v1，仅作粗规划，尚不能成为无条件完成承诺。以实际验收条件
是否闭合报告进度，不机械倒计时。主路线Issue2继续打开；仓库内可完成的模块继续
推进，真正外部验收则需要相应授权环境。密钥只留本机，不上传聊天或仓库。
