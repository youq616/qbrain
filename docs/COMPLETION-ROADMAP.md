# Qbrain 完成路线与阶段边界

更新2026-09-22。N48E整合此前A/B/C完整项目配置生命周期，完成Windows/Linux
原生测试及OpenCode1.18.31无账号配置加载/连接。N48D运行自检与旧回执/批量模块
保持。原路线按字节保留于[上一版本](COMPLETION-ROADMAP-N48D.md)。

## 本阶段关闭与未关闭的工作

关闭此前“OpenCode仅本地补丁、没有原生及实际宿主连接依据”的模块交付缺口。
固定V1客户端已有配置加载、连接、权限配置更新、schema补写处理、明确接管与卸载
证据。V2格式管理经过工程测试，V1兼容读取不能替代原生V2引擎或其阶段超时验收。

仍需在合适候选包中集成已接受新源码；公开N47X只包含N47S–N47W。不得用旧二进制
代表新命令，也不因完成配置接入就声称模型已经使用记忆。

## v1剩余验收工作流

| 工作流 | 尚需取得的证据 |
| --- | --- |
| 真实会话记忆消费及长期生命周期 | 在固定客户端中实际调用并在后续会话利用记忆；复用本轮已完成的配置连接证据，不重复计为未做 |
| 真实模型质量与费用 | 相同模型/设置、隔离答案与历史的实际回答和供应商用量，不能以回环响应替代 |
| 最终候选问题修复与集成 | 闭合实测问题、完整回归、统一包含新模块的候选版本并完成终验 |

历史3–4与15–25回合数字是有外部条件前提的粗规划，不是当前确定完成日期或
逐消息倒计时。本轮取得真实宿主连接结果，但没有新的真实模型回答/费用观察。
Issue40历史超时根因仍未知，配置生命周期测试不能替代其实际诊断。

完整增强路线继续包括PG新记忆模块对等、语义合并/冲突、确认与消费观测、画像/
衰减、完整ACL/DLP、更多明确需要的宿主、规模性能与正式签名。主路线保持打开。
当前不要求用户发密钥、改真实脑库或反复安装；必要本机任务仍遵循单提示词交接。
