# Qbrain 完成路线与阶段边界

记录2026-09-20 UTC。N47Z原子批量回执模块已经完成Windows/Linux源码验收。
N47X工程预览仍是当前公开下载；N47Y完整性修复和N47Z批量功能尚未进入公开包。
完整模块验收与整个项目完成是两件事，不按消息或测试数量递减剩余工作。

## 已完成的工程模块

N47X交付包含受控模型对照、单条使用记录、分页审计、路径保护和桥接诊断。
N47Y统一回执完整性，并修复MCP筛选/连续分页入口。N47Z新增只读计划、精确状态
绑定、跨事实批量上报/撤回、整批回滚及调用方事务所有权保护。单条命令、来源权限、
数据库结构和默认行为保持，不把记录上报冒充真实模型消费。

本轮完成的是已有长任务的仓库提交、原生验证、独立复核和合并，不再停留在本地
补丁阶段。新旧源码和二进制的具体边界见[当前状态](../CURRENT-STATUS.md)。

## 日常可用v1尚缺的三个验收工作流

| 工作流 | 完成证据 |
| --- | --- |
| 真实客户端生命周期与记忆消费 | 已登录目标客户端的后续会话实际使用；复用有效证据，只补缺失场景 |
| 真实模型对照与质量/费用 | 新资料、相同模型设置、隔离答案和历史，实际请求响应与供应商用量 |
| 最终候选修复与版本集成 | 根据实测问题修复、完整回归、纳入已接受源码并完成稳定版终验 |

历史v1约3–4个实质回合仅在必要账号/权限/模型和客户端环境已就绪、范围不扩大且
无重大返工时成立，不是确定剩余次数。内部模块完成不会补出未执行的外部观测。
Issue40历史超时根因仍未知，不以批量事务测试替代实际诊断。

## 完整增强目标

PG新记忆模块对等、通用语义合并/冲突、确认与消费观测、使用驱动画像/衰减、完整
ACL/DLP、更多明确需要的宿主、规模性能/全链路费用优化与正式签名仍属完整路线。
历史15–25回合包含v1，只是粗规划，不作无条件承诺。主路线Issue2保持打开。

当前模块未新增外发或付费调用，密钥只应保留在本机，不上传聊天或仓库。备份/WAL
安全擦除、自动修复损坏数据库、敌意管理员防护没有由这些模块隐式实现。
