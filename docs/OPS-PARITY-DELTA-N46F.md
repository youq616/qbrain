# N46F operation delta

Tested source c665cb29cb44a6827670b8910b3d6adb568aa2c1; all required jobs of
34765651987 passed and cjk-preview-c665cb29 was published. See the node outcome
and n46f-evidence/SUMMARY.json for exact tested bytes, counts and limitations.

No new MCP operations, schemas, runtime services or provider-consent changes.
SQLite search preserves existing FTS first and fills remaining candidates with
bound CJK literals in title/body/slug. Source, live-page and output bounds remain.
The memory_read algorithm remains unchanged; only its description is clarified.

WinHTTP now has one thread-safe immutable process session, request-local timeouts
and credentials, and final-callback parent lifetime. Source-matched Server 2022
and Server 2025 wire/cancellation checks passed; this is not endpoint-quality or
universal leak-freedom evidence. System proxy changes require process restart.

The exact native registry increased from 47 to 48 groups. Frozen old logs cannot
certify the new CJK package. No PostgreSQL CJK path, semantic segmentation, N47,
complete gbrain parity or whole-project completion is claimed.
