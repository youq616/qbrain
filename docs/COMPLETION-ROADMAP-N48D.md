# Qbrain 完成路线与阶段边界

更新2026-09-21。N48D隔离式MCP运行自检已完成跨平台源码验收。它关闭了“仅检查
配置或文件，尚未实际握手/退出”的运行自检缺口，不等于真实Agent或模型验收。
[上一版路线](COMPLETION-ROADMAP-N47Z.md)保留原字节，不删除历史未完成目标。

## 已完成与待集成的区别

公开N47X预览包含N47S–N47W；之后N47Y完整性、N47Z原子批量和N48D运行自检
完成源码模块验收，仍需在适当版本中统一发布，不能拿旧下载包验证新命令。
N48A/B/C的OpenCode配置安装、审计、外部编辑接管仍是另外未合并工作，本阶段
没有将其Windows或真实宿主门槛改写成通过。[当前状态](../CURRENT-STATUS.md)。

## 日常可用v1仍需闭合三个验收工作流

| 工作流 | 必要完成证据 |
| --- | --- |
| 真实客户端生命周期与记忆消费 | 已登录目标客户端加载正确配置，并在后续会话实际使用记忆；限定进程握手不替代该证据 |
| 真实模型对照与质量/费用 | 相同模型设置、隔离答案和历史的实际请求响应及供应商用量；不用测试响应器冒充模型效果 |
| 最终候选修复与版本集成 | 根据实测修复、运行完整回归、纳入最终接受源码并终验；保留未闭合问题 |

历史v1约3–4个实质回合仅适用于必要环境已就绪、范围不扩展且无重大返工，不是
确定完成次数。当前按验收工作流是否闭合报告进度，不按每次“继续”或测试数倒计时。
Issue40历史超时根因仍未知，N48D诊断观察不能倒推该历史原因。

## 完整增强路线

PG新记忆模块对等、通用语义合并/冲突、确认与消费观测、使用驱动画像/衰减、完整
ACL/DLP、更多明确需要的客户端、规模性能、全链路费用优化和正式签名仍在路线中。
历史15–25回合包含v1，仅是粗规划，不作无条件承诺。主路线Issue2继续打开。

当前不新增付费调用或真实会话外发。密钥只保留在本机；进程环境隔离不构成OS安全
沙箱，不把未知宿主、PG、签名结果或数据安全擦除写成已经实现。
